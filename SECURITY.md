# Security policy

Never commit AWS credentials, MongoDB passwords, JWT secrets, kubeconfigs, private keys, or Terraform state. Use protected Jenkins credentials, AWS IAM roles, Kubernetes Secrets/External Secrets, or another dedicated secret manager.
